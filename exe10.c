#include <stdio.h>
int main() {
    printf("size de int est: %zu\n", sizeof(int));
    printf("size de float est: %zu\n", sizeof(float));
    printf("size de double est: %zu\n", sizeof(double));
    printf("size de char est: %zu\n", sizeof(char));
    printf("size de short est: %zu\n", sizeof(short));
    printf("size de long est: %zu\n", sizeof(long));
    printf("size de long double est: %zu\n", sizeof(long double));
    return 0;
}